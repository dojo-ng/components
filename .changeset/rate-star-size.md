---
"@dojo-ng/rate": patch
---

Fix stars rendering at the UA button font size instead of the star box size, which made a row of 24px stars look like a row of dots. Added `--dj-rate-size` (default `1.5rem`) to drive the star button's width, height, and font-size together.
